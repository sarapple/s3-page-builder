/*
  To include more script files:
  1. Add the custom js file into the ./src folder
  2. Add the path (folders included) to the customJs array in sources.json
  3. Run gulp to build. File should be concatenated (based on array order) and minified.
*/

console.log('javascript working');
